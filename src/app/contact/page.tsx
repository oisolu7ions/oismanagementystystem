import { PublicPlaceholderPage } from "@/components/legal-support/public-placeholder-page";

export const metadata = {
  title: "Contact",
};

export default function ContactPage() {
  return (
    <PublicPlaceholderPage
      title="Contact"
      description="Contact details and support options for OIS clients will be added here."
    />
  );
}
